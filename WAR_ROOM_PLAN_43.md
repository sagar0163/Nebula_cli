# Issue 43: Create Plugin System: Community-Contributed Healing Patterns

- [x] 1. Create plugin sandbox (`src/plugins/sandbox.js`) to prevent malicious code execution.
- [x] 2. Create plugin API (`src/plugins/api.js`) to expose necessary functions to plugins.
- [x] 3. Create plugin registry (`src/plugins/registry.js`) for managing (loading, resolving, registering) plugins.
- [ ] 4. Create testing framework (`src/plugins/testing.js`) for testing plugins.
- [ ] 5. Implement built-in `docker` plugin (`src/plugins/builtin/docker.js`).
- [ ] 6. Implement built-in `git` plugin (`src/plugins/builtin/git.js`).
- [ ] 7. Implement built-in `npm` plugin (`src/plugins/builtin/npm.js`).
- [ ] 8. Implement plugin management commands (`src/commands/plugin.js`) for CLI.
- [ ] 9. Export main plugin modules (`src/plugins/index.js`).
- [ ] 10. Integrate plugin commands into CLI entry point (`src/index.js` or equivalent).
- [ ] 11. Write a test file for the plugin system to ensure it meets acceptance criteria (`test/unit/plugin.test.js` or similar).
